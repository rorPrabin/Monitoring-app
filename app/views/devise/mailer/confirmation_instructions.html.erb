<p>Welcome <%= @email %>!</p>

<p>You can confirm your account email through the link below:</p>
<% @url = confirmation_url(@resource, :confirmation_token => @resource.confirmation_token, :only_path => false) %>
<p><%= link_to 'Confirm my account', @url %></p>
